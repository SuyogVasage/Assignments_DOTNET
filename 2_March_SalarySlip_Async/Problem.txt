Modify the EMployee-Salary SLip application by generating Salary Slip for 
each EMployee on a Task, once these files are generated copy all files 
from C:\Source to C:\Destination folder with Task Continue


Makse sure that 1 and 2 uses Exception Handling






Explore Task.FromException() method

>> This method creates a Task object whose Status property is Faulted and whose Exception 
property contains exception. The method is commonly used when you immediately know that the
work that a task performs will throw an exception before executing a longer code path. 
For an example, see the FromException<TResult>(Exception) overload.